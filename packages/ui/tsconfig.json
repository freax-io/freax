{
	"extends": ["@freax/tsconfig/base.json"],
	"compilerOptions": {
		"rootDir": "src", // <-- added so we don't get src/
		"outDir": "./dist",
		"noEmit": false,
		"tsBuildInfoFile": "dist/tsconfig.tsbuildinfo"
	},
	"include": ["src/**/*"]
}